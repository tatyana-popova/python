#задаем время обучения
time_to_learn = 40

def clock(time_to_learn):
  # если время обучения равно 40 мин
  if time_to_learn == 40
  # выводим сообщение "Пора смотреть сериал"
    print "Пора смотреть сериал"
  # если время обучения меньше 40 мин
  else time to learn < 40
  # выводим сообщение "Еще рано смотреть сериал, надо позаниматься"
    print "Еще рано смотреть сериал, надо позаниматься"



#создаём переменную, которая хранит количество времени, отведённое на обучение
timer_seconds = 10

#объявляем функцию, которая будет отсчитвать время, заданное в переменной
def my_timer():
    # Строчка ниже говорит таймеру, сколько секунд ждать
    time.sleep(timer_seconds)
    # Как только этот таймер (sleep) заканчивается, запускается сообщение
    print("Пора смотреть сериал!") 
